{
  "modes": {
    "CONTROLLED": {
      "purpose": "same-seed forward reproducibility",
      "seed_applied": true
    },
    "UNSEEDED_DIAGNOSTIC": {
      "purpose": "detect random initialization of missing trainable parameters",
      "seed_applied": false
    }
  },
  "hard_block": "MODEL_LOAD_REPRODUCIBILITY_HARD_BLOCK",
  "fingerprints": [
    "model_state",
    "trainable",
    "buffer"
  ],
  "inspect_real_model_object": true,
  "aggregate_gate": "PASS only when complete model-state, trainable-parameter, and buffer fingerprints are identical across all five fresh processes; loader-reported main-checkpoint missing keys remain in evidence because frozen mhr_model.pt is a separate TorchScript source"
}
