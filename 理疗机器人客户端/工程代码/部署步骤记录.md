# 理疗机器人客户端部署步骤记录

> 这是本项目部署工作的唯一进度账本。每完成一个动作，必须记录日期、操作、验证证据、遗留问题和下一步；未经验证的事项不得标记为完成。

## 当前状态

- 更新时间：2026-08-11
- 当前阶段：第一阶段 Docker 本机演示版已运行；步骤 11 的备份恢复、依赖安全、定时备份和开机自恢复子项已完成
- 当前系统：Windows 11 Pro 25H2，OS Build 26200.8875，已激活
- 当前可运行版本：Python 3.11 本地版，以及 Docker 前端 + 后端第一阶段演示版
- 完成边界：已复现第一阶段客户端运行链路；尚未完美复现完整产品、真实机器人控制或联网生产环境
- 下一动作：接入独立物理盘或网络存储后补异盘副本，再决定局域网和防火墙范围
- 已知待办：异盘备份与断电测试未完成；局域网尚未开放；Secure Boot 当前关闭；真实机器人通信尚未接入

## 记录规则

1. 一个步骤只有在操作和验证都完成后才能标记为 `已完成`。
2. 账号密码、令牌和密钥只记录存放位置，不把真实值写入本文档。
3. 需要重启、删除、覆盖、开放公网端口或修改安全策略时，先记录风险和备份位置。
4. 失败尝试也要记录原因和处置，防止以后重复踩坑。
5. `DEPLOY.md` 记录产品部署说明；本文档只记录这台电脑的实际执行进度和证据。

## 总体路线

| 步骤 | 内容 | 状态 |
|---|---|---|
| 1 | 定位和检查工程代码 | 已完成 |
| 2 | 检查电脑环境和第一阶段可部署性 | 已完成 |
| 3 | 安装 Python 3.11、依赖并初始化数据库 | 已完成 |
| 4 | 配置 PyCharm 并验证本地演示版 | 已完成 |
| 5 | 升级 Windows 11 前检查和备份 | 已完成 |
| 6 | 升级并验证 Windows 11 Pro 25H2 | 已完成 |
| 7 | 启用 WSL 2 并安装 Ubuntu | 已完成 |
| 8 | 安装 Docker Desktop，数据目录放到 E 盘 | 已完成 |
| 9 | 修整 Dockerfile、Compose、环境变量和数据库初始化 | 已完成 |
| 10 | 构建并验证第一阶段 Docker 版本 | 已完成 |
| 11 | 局域网、安全、备份与开机启动配置 | 进行中（本机备份、安全、自启动已完成） |
| 12 | 接入真实机器人 SDK/协议和安全联锁 | 等待机器人资料 |

## 详细执行记录

### 步骤 1：定位和检查工程代码

- 状态：已完成
- 日期：2026-08-10
- 项目目录：`E:\项目-按摩理疗机器人\理疗机器人客户端\工程代码`
- 识别结果：Vue 3/Vite 前端、FastAPI/SQLite 后端，根目录包含 Docker Compose 和构建脚本。
- 结论：现有设备和理疗服务主要修改数据库状态；AI 分析为模拟逻辑；真实机器人通信尚未连接。

### 步骤 2：检查电脑环境和第一阶段可部署性

- 状态：已完成
- 日期：2026-08-10
- 硬件：AMD Ryzen 5 9600X、31.2 GB 内存、固件虚拟化已开启。
- 磁盘：项目和后续 Docker 数据使用 E 盘；系统盘空间需要持续监控。
- 结论：可以运行本地演示和 WSL 2/Docker；原 Windows 10 不适合作为长期联网生产环境。

### 步骤 3：安装 Python、依赖和数据库

- 状态：已完成
- 日期：2026-08-10
- Python：`C:\Users\Administrator\AppData\Local\Programs\Python\Python311\python.exe`
- 虚拟环境：`backend\.venv`
- 依赖修正：为兼容 `passlib 1.7.4`，在 `backend\requirements.txt` 固定 `bcrypt==4.0.1`。
- 数据库：`backend\data\physiotherapy.db`
- 验证：数据库初始化成功；健康检查、首页、静态资源、Swagger、登录、个人资料、设备列表和理疗方法接口均通过。

### 步骤 4：配置 PyCharm 和本地演示

- 状态：已完成
- 日期：2026-08-10
- PyCharm：2026.2.0.1，安装在 E 盘。
- 新增入口：`backend\run_first_stage.py`
- 运行配置：`.run\第一阶段-FastAPI.run.xml`
- 使用说明：`PYCHARM使用教程.md`
- 验证：`http://127.0.0.1:8000`、`/docs` 和 `/health` 已实际演示。

### 步骤 5：Windows 11 升级前检查和备份

- 状态：已完成
- 日期：2026-08-10
- TPM：存在、就绪、启用并激活。
- 系统盘：GPT；BitLocker 未启用；虚拟化已启用。
- 主板：ASUS B650M-AYW WIFI，BIOS 3263。
- 备份位置：`E:\项目-按摩理疗机器人\升级备份\20260810-Windows11升级前`
- 备份验证：SQLite 原文件与备份 SHA-256 一致。
- 发现并处置：原 Windows Update 服务和策略被锁定；最终由 Windows 11 官方安装助手完成升级并重建更新组件。

### 步骤 6：升级和验证 Windows 11

- 状态：已完成
- 日期：2026-08-10
- 安装来源：微软官方 Windows 11 Installation Assistant，数字签名验证为 Microsoft Corporation。
- 版本：Windows 11 Pro 25H2，OS Build 26200.8875。
- 激活：已激活。
- 升级后服务：Windows Update、BITS、Update Orchestrator、Cryptographic Services 均已运行。
- 系统盘升级后可用空间：约 21.6 GB。
- 遗留：Secure Boot 仍为关闭状态，后续在进入联网生产前开启并复查。

### 步骤 7：启用 WSL 2 并安装 Ubuntu

- 状态：已完成
- 日期：2026-08-10
- 升级后初检：`wsl.exe` 存在，但 WSL 功能尚未安装，没有 Linux 发行版。
- 计划：启用 WSL 和虚拟机平台，安装最新 WSL 组件，重启后安装 Ubuntu，并确认发行版版本为 WSL 2。
- 验证标准：`wsl --version` 可用；`wsl -l -v` 显示 Ubuntu 且 VERSION 为 2；Ubuntu 能执行基本网络和文件系统命令。
- 执行记录 1：尝试用一次命令安装 WSL 和 Ubuntu；未修改系统。原因是 Windows 自带的旧版 `wsl.exe` 尚不支持 `--distribution`、`--location` 等新参数，且错误输出触发脚本提前退出。
- 执行记录 2：把 `运维脚本\install_wsl.ps1` 改成可重复运行的两阶段脚本。
- 执行记录 3：已启用 `Microsoft-Windows-Subsystem-Linux` 和 `VirtualMachinePlatform`；两项结果均为 `Enabled`，系统检测到待重启。
- 执行记录 4：Windows 已重启；复查两项功能均为 `Enabled`，系统无待重启状态。
- 执行记录 5：重启后 `wsl --status`、`wsl --version` 和 `wsl -l -v` 仍提示未安装 WSL，说明 Windows 功能已就绪，但新版 WSL 运行时/应用包尚未安装。
- 执行记录 6：按旧版 `wsl.exe` 的兼容情况增强脚本：优先使用 Web 下载更新，失败时改走标准更新通道，并在安装 Ubuntu 前单独验证 `wsl --version`。
- 执行记录 7：两个内置更新通道均被远端以 HTTP 403 拒绝，未继续执行半成功安装。随后按微软官方离线安装路线，从 `microsoft/WSL` Release 下载稳定版 `wsl.2.7.11.0.x64.msi`。
- 安装包验证：258,990,080 字节；SHA-256 为 `A611DDACEE689D2FB1FB5319E58AF7F3998864D86CDCE632EADD8E61614A0F9D`，与官方 Release API 一致；Authenticode 状态为 `Valid`，签名者为 Microsoft Corporation。
- 执行记录 8：WSL 2.7.11.0 MSI 安装成功，内核版本为 6.18.33.2-2；默认发行版版本已设为 WSL 2。
- 执行记录 9：Ubuntu 24.04 安装到 `E:\WSL\Ubuntu-24.04`，发行版注册名为 `Ubuntu-24.04`，虚拟磁盘为 `E:\WSL\Ubuntu-24.04\ext4.vhdx`。
- 最终验证：`wsl -l -v` 显示 `Ubuntu-24.04` 的 VERSION 为 2；Ubuntu 报告 `Ubuntu 24.04.4 LTS`；`/mnt/e` 可访问；DNS 可解析 GitHub，HTTPS 返回 200。
- 非阻塞提示：WSL 启动时提示 localhost 代理未镜像到 NAT；WinHTTP 为直连，Ubuntu DNS/HTTPS 实测正常，后续 Docker 拉取镜像时继续观察。
- 日志：`运维脚本\logs\install-wsl.log`
- 当前停点：步骤 7 已完成，进入 Docker Desktop 安装前检查。

### 步骤 8：安装并验证 Docker Desktop

- 状态：已完成
- 日期：2026-08-10
- 前置检查：`LanmanServer` 正在运行且为自动启动；安装前 C 盘约 104.2 GB、E 盘约 324.8 GB 可用；未发现旧 Docker 安装或 `E:\Docker` 数据目录冲突。
- 安装来源：Docker 官方 CDN，Docker Desktop 4.86.0，Windows x64 构建号 236216。
- 安装包验证：628,979,632 字节；SHA-256 为 `820438E75C16E44B393079154BEA7D27958A15845C23A635B1A1F6F586B2ED44`，与官方校验文件一致；Authenticode 状态为 `Valid`，签名者为 Docker Inc。
- 安装方式：当前用户模式、WSL 2 后端、禁用 Windows 容器；未在脚本中自动接受 Docker 订阅服务协议。
- 安装脚本：`运维脚本\install_docker_desktop.ps1`；固定版本安装包已按清场确认删除，重装前必须重新下载并通过脚本内的文件名、SHA-256 和签名校验。
- 程序位置：`C:\Users\Administrator\AppData\Local\Programs\DockerDesktop`
- 数据位置：`E:\Docker\wsl`；已验证 `disk\docker_data.vhdx` 和 `main\ext4.vhdx` 均实际生成在 E 盘。
- 组件版本：Docker Desktop 4.86.0；Docker Engine/CLI 29.7.2；Docker Compose 5.3.1。
- WSL 验证：`docker-desktop` 正在运行且 VERSION 为 2；容器后端为 Linux/amd64。
- 容器验证：成功从 Docker Hub 拉取并运行官方 `hello-world:latest`，退出码为 0。
- 处置记录：首次命令行验证因当前 PowerShell 的 PATH 尚未刷新而找不到 `docker-credential-desktop`；仅在验证进程内临时加入 Docker `resources\bin` 后通过，未改写系统全局 PATH。
- 日志：`运维脚本\logs\install-docker-desktop.log`
- 当前停点：步骤 8 已完成，进入项目容器配置审计和修整。

### 步骤 9：修整项目容器配置

- 状态：已完成（静态语法、Compose 展开和自助脚本自检通过；实际构建归步骤 10）
- 日期：2026-08-10
- 安全修整：后端不再把真实 `.env` 复制进镜像；Compose 不再使用默认生产密钥；`.env.docker` 由启动脚本首次随机生成且不显示真实值。
- 暴露范围：仅前端绑定 `127.0.0.1:8080`；后端 8000 端口只在 Compose 内部网络暴露。
- 构建修整：后端对齐 Python 3.11 并使用非 root 用户；前端使用 Node 22 和 `npm ci`；增加前后端健康检查及启动依赖。
- 数据初始化：增加 `SEED_DEMO_DATA` 开关，本机第一阶段容器版显式开启；数据库初始化失败会返回非零结果。
- 可复现脚本：`运维脚本\start_docker_first_stage.ps1`；回滚 DNS 脚本为 `运维脚本\restore_network_dns.ps1`。
- 静态验证：PowerShell、Python 和 Compose 语法通过；Compose 展开结果确认只有 `127.0.0.1:8080` 对主机发布；8080 端口可用。
- 构建尝试 1：Docker Hub 授权请求解析到不可达 IPv6 地址，Python/Node/Nginx 三个官方基础镜像均未开始下载。
- 处置尝试：Docker Desktop 设置为 `defaultNetworkingMode=ipv4only`、`dnsInhibition=ipv6` 并重启，设置生效但未解决主机侧 Docker Hub 认证连接。
- 构建尝试 2：仍在 `auth.docker.io` 获取匿名令牌时失败；没有创建或启动项目容器。
- 网络证据：DHCP DNS `172.18.1.92/93` 将 `auth.docker.io` 返回到 Facebook/Twitter 网段；可信 DoH 返回 Cloudflare 地址；即使把域名固定到正确 Cloudflare IP，TLS 连接仍被网络主动重置（curl 35）。
- DoH 验证：尝试腾讯 DoH 后 Docker Hub HTTPS 仍失败，脚本已自动回滚；最终执行恢复脚本，网卡 DNS 已恢复为原 DHCP 配置，测试用 DoH 注册也已移除。
- 当时阻塞：校园网直连同时干预 DNS 和 Docker Hub TLS；该阻塞已在步骤 10 通过 FlClash 本机代理解决。
- 自助交接：新增根目录 `Docker_One_Click.cmd` 和 `docker_easy_menu.ps1`，独立脚本可完成网络检测、镜像拉取、构建、健康检查和打开网页。
- 菜单能力：第一次构建/重建、启动、停止（保留数据卷）、状态、打开网页和最近日志；每次运行日志写入 `logs\docker-easy-menu-日期时间.log`。
- 教程：新增 `Docker傻瓜式操作教程.md`；`DEPLOY.md` 已同步为当前 `127.0.0.1:8080`、`.env.docker` 和 `docker compose` 流程，旧端口及旧密钥说明已移除。
- 步骤 9 当时验证：PowerShell 语法通过；可执行脚本为纯 ASCII；`-Action selftest` 找到 Docker CLI、Compose 和现役启动脚本并通过；当时的 `-Action status` 正确显示尚无项目容器、健康地址不可达。
- 日志：`运维脚本\logs\network-dns-change.log`
- 当前停点：步骤 9 已完成；步骤 10 的运行事实见下节。

### 步骤 10：构建并验证第一阶段 Docker 版本

- 状态：已完成
- 日期：2026-08-10
- 网络路线：检测到 FlClash/FlClashCore 的 HTTP 代理监听 `127.0.0.1:7890`；通过该代理访问 Docker Hub 认证端点返回 HTTP 200。
- 系统代理：当前 Windows 用户的代理设为 `127.0.0.1:7890`，本机地址列入绕过清单；未修改 DNS 或 WinHTTP 全局配置。
- 基础镜像：`python:3.11-slim`、`node:22-alpine` 和 `nginx:1.28-alpine` 官方镜像均已成功拉取。
- 构建结果：`physiotherapy-client-backend:latest` 和 `physiotherapy-client-frontend:latest` 构建成功；pip 和 npm 依赖下载完成。
- 运行结果：后端与前端容器均已启动并显示 `healthy`；后端仅在 Compose 网络暴露 `8000/tcp`，前端仅绑定 `127.0.0.1:8080->80/tcp`。
- 在线验证：`http://127.0.0.1:8080/health` 返回 HTTP 200 和 `{"status":"ok"}`；首页返回 HTTP 200。
- 一键验收：修改后的 `Docker_One_Click.cmd -Action deploy` 从代理检测、镜像检查、缓存重建到健康检查全链路退出码为 0，并打印 `DEPLOYMENT PASSED`；日志为 `logs\docker-easy-menu-20260810-230956.log`。
- 数据：创建并使用 `backend-data`、`backend-uploads`、`backend-logs` 三个命名数据卷；未删除任何既有数据。
- 自助脚本修整：菜单 `1` 改为自动尝试直连和 FlClash `7890`，构建时保持 FlClash 已连接；菜单 `2` 使用 `--no-build --pull never` 离线启动已有镜像。
- 当时告警：`npm ci` 报告 10 个依赖漏洞（2 个中危、8 个高危）；已于 2026-08-11 在步骤 11 完成升级和回归，当前为 0 个漏洞。
- 当前停点：步骤 10 已完成，进入步骤 11 的备份恢复、局域网和安全配置。

### 步骤 11：备份恢复、局域网与安全配置

- 状态：进行中（SQLite/Docker 卷备份恢复演练已完成；其余子项待执行）
- 日期：2026-08-11
- 新增入口：`Docker_Backup_Restore.cmd`；实现脚本为 `docker_backup_restore.ps1`，教程为 `Docker备份恢复教程.md`。
- 在线备份：通过 SQLite `backup()` 从运行中的 `physiotherapy-client_backend-data` 生成一致性副本，没有停止后端或覆盖现役数据。
- 备份文件：`backups\docker-sqlite\physiotherapy-20260811-143750.db`，大小 65,536 字节。
- SHA-256：`2962AF12FBC261F8DB8D7BF2947C077AB56E96DA7DD8EB82D38172087A14D658`；与清单及隔离恢复后的文件一致。
- 数据验证：SQLite `integrity_check` 返回 `ok`；7 张业务表均可读取；记录数量与备份清单一致。
- 恢复演练：备份已恢复到随机命名的临时 Docker 卷，并在独立 `python:3.11-slim` 容器内完成完整性、哈希、表和记录数验证。
- 安全结果：临时验证容器和临时卷均已删除；现役卷未修改；前后端容器演练后仍为 `healthy`，`/health` 返回 `{"status":"ok"}`。
- 证据：同名 `.db.json` 清单、`restore-drill-20260811-143751.json` 演练记录和 `logs\docker-backup-20260811-143749.log`。
- 依赖审计基线：npm 官方审计报告 10 个漏洞（2 个中危、8 个高危），直接漏洞链为 Axios 和 Vite，另有未配置且未被脚本引用的 ESLint 8。
- 安全升级：Axios 从 1.14.0 升至 1.19.0，Vite 从 5.4.21 升至 6.4.3；现有 `@vitejs/plugin-vue` 5.2.4 与 Vite 6 的 peer 范围兼容；移除未使用的 ESLint 8。
- 传递依赖：通过非强制 `npm audit fix --package-lock-only` 将 Immutable 升至 5.1.9、nanoid 升至 3.3.18、PostCSS 升至 8.5.26，并更新 Axios/Vite 相关依赖。
- 审计结果：`npm audit` 和 Docker 构建内的 `npm ci` 均报告 0 个漏洞；未使用 `--force`，没有修改业务源码。
- 部署回归：Vite 6.4.3 生产构建成功，前端容器已重建并为 `healthy`；首页加载新资源 `/assets/index-AoAQbdd6.js`；`/health`、演示账号登录和用户资料 API 全部通过。
- 磁盘边界：C、D、E 均位于物理磁盘 0（ZHITAI TiPlus7100 1TB NVMe），当前没有独立备份盘；跨分区复制不能防整盘故障。
- 登录启动：注册 `Physiotherapy Client - Start Docker at Logon`，使用当前用户登录触发 `docker desktop start`；手动试跑返回 0。Compose 已有 `restart: unless-stopped`。
- 定时备份：注册 `Physiotherapy Client - Daily Database Backup`，每天 20:00 调用现役备份脚本；启用错过后补跑、15 分钟上限和禁止并发，不自动删除旧备份。
- 计划任务验收：2026-08-11 15:00:45 后台试跑返回 0，生成 `physiotherapy-20260811-150046.db`；大小 65,536 字节，完整性为 `ok`，清单 SHA-256 与文件一致，新日志不含 `SECRET_KEY`。
- 安全处置：首次计划任务因 Windows PowerShell 后台编码导致完整 `docker inspect` JSON 解析失败，失败日志意外记录容器环境变量。已删除精确日志 `logs\docker-backup-20260811-145758.log`，轮换 `.env.docker` 的 JWT 密钥并重建容器；旧登录令牌失效，账号与数据库未改变，重新登录和 `/health` 验证通过。
- 根因修复：备份脚本不再读取包含环境变量的完整容器 JSON，只用 Docker 格式化输出读取运行状态、健康状态和数据卷名；复跑日志已确认无密钥。
- 实际重启：Windows 于 2026-08-11 15:15:36 完成重启；`Physiotherapy Client - Start Docker at Logon` 在 15:15:44 运行且结果为 0。
- 重启后自动验收：15:16:51 生成 `logs\post-reboot-verify.json`，确认启动时间已变化、Docker 已就绪、前后端均为 `running/healthy`、`/health` 返回正常且首页 HTTP 200。
- 独立复验：Docker Desktop 状态为 `running`；Compose 显示两个容器自动恢复；健康检查和演示账号重新登录通过；一次性验证任务已自注销。
- 收尾清理：已删除一次性 `verify_after_reboot.ps1` 和重启前时间标记，仅保留不含密钥的验证报告作为证据。
- 待完成：接入独立物理盘/NAS 后配置异盘副本、断电测试、局域网/防火墙配置、Secure Boot 复查。

## 清场记录

- 日期：2026-08-10
- 用户确认：在完整部署与待清理清单汇报后，用户明确回复“确认清除”。
- 已删除：`运维脚本\downloads\docker-desktop-4.86.0.exe`（628,979,632 字节）。
- 已删除：`运维脚本\downloads\wsl.2.7.11.0.x64.msi`（258,990,080 字节）。
- 已删除：`backend\docker-compose.yml`（404 字节，已由根目录现役 Compose 取代）。
- 释放空间：合计 887,970,116 字节，约 846.8 MiB。
- 删除后验证：三个精确路径均不存在；没有使用通配符或递归删除，没有停止容器、删除镜像、数据卷、数据库或部署日志。
- 重装影响：`运维脚本\install_docker_desktop.ps1` 仍会校验固定文件名和 SHA-256；如果以后重装 Docker Desktop，必须先重新下载对应官方安装包到 `运维脚本\downloads`。WSL 重装也需要重新获取官方 MSI。
- 保留：下载错误日志、安装日志、一键部署验收日志和当前容器运行现场，供后续复查。

## 知识收尾核验

- 日期：2026-08-11
- 权威顺序：当前 Docker 运行态和根目录 `docker-compose.yml` 优先；现役操作说明为 `DEPLOY.md`、`Docker傻瓜式操作教程.md`、`Docker备份恢复教程.md` 和 `backend\README.md`；本文档记录本机执行证据。
- 运行复验：`physiotherapy-client-backend-1` 与 `physiotherapy-client-frontend-1` 均为 `healthy`；前端仅绑定 `127.0.0.1:8080`；`/health` 返回 `{"status":"ok"}`，首页返回 HTTP 200。
- 配置复验：Compose 展开检查通过；根目录是唯一现役 Compose 入口；Docker 后端 8000 端口未发布到 Windows 网卡。
- 规则与文档：`AGENTS.md`、`DEPLOY.md`、Docker 教程、备份教程和后端 README 已与步骤 11 当前运行事实对齐；旧设计稿保留显眼的历史资料提示，不作为现役操作说明。
- 完成结论：步骤 10 已完成，步骤 11 的备份恢复、前端依赖安全、定时备份和开机自恢复子项已完成；这不等于完整机器人产品已完美复现，也不等于生产部署完成。
- 演练清理：只删除本次创建的临时验证容器和临时卷；现役数据库、数据卷、备份和日志均保留。

## 下一步检查清单

- [x] 启用 WSL 功能
- [x] 启用 Virtual Machine Platform
- [x] 按要求重启 Windows
- [x] 安装或更新 WSL 组件
- [x] 安装 Ubuntu 到规划位置
- [x] 验证 Ubuntu 使用 WSL 2
- [x] 安装 Docker Desktop 并验证容器运行
- [x] 审计并修整项目容器配置
- [x] 通过 FlClash 本机代理建立可访问 Docker Hub 的网络路线
- [x] 构建并验证第一阶段项目容器
- [x] 演练 SQLite/Docker 命名卷备份与恢复
- [x] 审计并修复前端依赖漏洞告警
- [x] 配置并试跑每天 20:00 的自动数据库备份
- [x] 配置并试跑当前用户登录时启动 Docker Desktop
- [ ] 接入独立物理盘或网络存储并配置异盘副本
- [x] 实际重启验证 Docker 和项目容器自动恢复
- [ ] 配置局域网访问、防火墙白名单和开机启动
- [ ] 开启并复查 Secure Boot
- [ ] 获取并接入真实机器人 SDK/协议和安全联锁资料
