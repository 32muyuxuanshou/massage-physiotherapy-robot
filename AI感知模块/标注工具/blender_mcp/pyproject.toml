[project]
name = "acupoint-blender-mcp"
version = "0.2.1"
description = "受控的 SKEL 穴位标注 Blender MCP 服务"
requires-python = ">=3.11"
dependencies = [
  "mcp==2.1.1",
  "Pillow==12.1.1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
